version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"   

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
      pip_install_args:
        - --no-deps
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
