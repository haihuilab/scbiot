# Preprocessing: `pp`

Preprocessing helpers are internal details that support the workflows shown in
the tutorials and may change without notice. The only public entry point
documented in the API reference is `scbiot.ot.integrate`, so rely on the
tutorials for end-to-end recipes that include preprocessing steps.
